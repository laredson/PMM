# PMM 1.3.0 RC27 — AIIO local-first release candidate

Build: `PMM-v1.3.0-RC27-AIIO-LOCAL-FIRST`  
Product: Palworld Manager Merger  
Creator: laredson

## Delivered

- New top-level **AI & Help** tab with Help diagnostics, persistent AIIO sessions, Knowledge/storage/recovery and the color-scheme editor.
- Manual AIIO v2 ZIP exchange with incremental capability requests and strict untrusted-response staging; preparation, data fulfillment, import and candidate validation run through the supervised background worker.
- Exact candidate activation boundary: only current `PMM_MANUAL_SOLUTION_V1` cooked families may be explicitly submitted to Merge validation; Analyze is forced and Build/Deploy remain separate.
- Recoverable operation journal, save-activity evidence, local diagnostic cases, background artifact-inventory refresh and conservative disposable cleanup review.
- Deterministic compatibility-build IDs, immutable local validation events and inspectable feedback JSON without upload.
- Eleven official JSON themes plus Night/Light, separated from user schemes like built-in/custom sounds.
- Local theme editor with color and optional PNG/JPEG image for every palette and ColorFlow brush; V1/V2 export and offline AI theme requests.
- Equal responsive header halves, larger transparent logo/title and retained 900×600/high-DPI behavior.
- Confirmed 100% progress jumps immediately.

## Preserved contracts

RC27 retains effective-patch reuse, PowerShell singleton guards, Gura variant preflight, the exact FasterMounts/RushRoar semantic rule and every RC24 Fix Lab deployment boundary. **Apply Fix**, Restore and source-mod operations cannot alter a deployed compatibility patch.

## Trust boundary

RC27 has no provider login, remote upload, startup consent prompt, identity reset, arbitrary shell/code execution or automatic publishing. AI content is data. Apply Fix, candidate activation, Build, Deploy, Restore, contribution creation and publication remain explicit user actions.

## Acceptance gate

Static validation can prove schemas, hashes, localized XAML parity, package integrity and safety invariants. A Windows acceptance pass is still required for WPF layout/DPI, audio, external tools, Palworld deployment and in-game semantics. Follow `TEST_THIS_BUILD_RC27.txt` before publishing.
