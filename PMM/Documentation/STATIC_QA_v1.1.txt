# Palworld Manager Merger v1.1 static QA

**Date:** 2026-08-18

This is the Linux/container-side static gate for the priority-UX release candidate. The authoritative Windows gate remains `SmokeTest.ps1` (PowerShell parser + source/runtime-contract checks), dependency self-tests, WPF rendering, and the Palworld runtime test.

```text
PASS 157 / FAIL 0
```

Validated in this container:

- all three XAML files are XML-well-formed and EN/ES contain all 84 controls bound by `Start-PalModMerger.ps1`;
- merge-plan schema is 14 and build-manifest schema is 8;
- `Data/mod-priorities.json` is unique and covers all 43 source PAKs in the supplied library;
- absolute numeric ordering uses insertion semantics and reproduces the required `8 -> 1` and `8 -> >N` behaviors;
- drag/drop before/after position formulas were exhaustively checked for small/medium test lists;
- drag threshold, drop handlers and edge auto-scroll markers are present;
- language selection is data-backed and ComboBox/ComboBoxItem sizing/typography is explicit;
- dynamic conflict-decision ComboBoxes explicitly use the same height/font/padding contract;
- priority changes still alter merge-order identity without changing source-set identity;
- manual priority override / Unsupported safety markers remain present;
- every `Tools/PMMCore` file is byte-for-byte identical to the preceding priority RC;
- all merge-oriented `Core/*.ps1` files except the intentionally changed `LibraryService.ps1` are byte-for-byte unchanged from that RC;
- lightweight delimiter scanning found no unmatched PowerShell braces/brackets/parentheses/strings;
- one-off Early Aquatic investigation material was removed from the publication candidate because the new-world test passed.

Windows still must run `SmokeTest.ps1`, which now checks the new drag/direct-order UI contract instead of the obsolete read-only `Binding="{Binding Priority}"` marker. Static Linux checks do not replace a real PowerShell parser, WPF render, or in-game validation.
